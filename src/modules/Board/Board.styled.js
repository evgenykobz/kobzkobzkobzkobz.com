import styled from 'styled-components';

export const BoardStyled = styled.div`
  scroll-snap-type: y mandatory;
  max-height: 100%;
  overflow-y: auto;
  overflow-x: hidden;
  position: relative;
`;
