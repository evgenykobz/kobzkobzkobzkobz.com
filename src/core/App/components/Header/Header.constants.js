// Width (width equals height) settings
export const INITIAL_WIDTH = 150;
export const INITIAL_SCALE = 1;
export const TARGET_SCALE = 0.5;

// Trigger points settings
export const INITIAL_SCROLL = 0;
export const TARGET_SCROLL = 200;

// Opacity settings
export const INITIAL_OPACITY = 0;
export const TARGET_OPACITY = 0.75;

export const FILL_VARIANTS = {
  black: 'black',
  white: 'white',
  default: 'default',
};
